name: 'Run test suite'
inputs:
  python-version:
    required: true
    default: 3.8
  outputs:
    results:
      description: test results
  runs:
    using: 'docker'
    image: 'Dockerfile'
    args: .inside/test.sh ${{ inputs.python-version }}

